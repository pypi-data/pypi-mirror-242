[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
name = "juxt"
requires-python = ">=3.8"
version = "0.0.1"

[project.optional-dependencies]
dev = ["ruff", "pyright"]

[tool.ruff.lint]
select = ["I"]

[tool.pyright]
strict = ["src/juxt"]
