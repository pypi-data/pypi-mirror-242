[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "v2v"
authors = [{name = "404Vector", email = "tiryul@gmail.com"}]
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version", "description"]
requires-python = ">=3.9"
dependencies = [
  "ffmpeg-python",
  "numpy",
]

[project.urls]
Home = "https://github.com/404Vector/Video2Video"
