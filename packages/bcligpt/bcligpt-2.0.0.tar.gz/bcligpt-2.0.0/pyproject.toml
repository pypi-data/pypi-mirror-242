[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[project]
name = "bcligpt"
version = "2.0.0"
description = "Better CLI for GPT"
dependencies = [
    "openai", "tiktoken", "rich"
]
readme = "README.md"
authors = [
    { name = "Marco Filippozzi", email = "marco.filippozzi98@gmail.com" }
    ]
[project.scripts]
bcligpt = "bcligpt:start"
