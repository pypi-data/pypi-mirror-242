[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mutation"
version = "0.4.8"
description = "Mutation testing made easy"
readme = "README.md"
requires-python = ">=3.10"

[console_scripts]
mutation = "mutation:__main__"

