[tool.poetry]
name = "pypixyz"
version = "0.1.0"
description = ""
authors = ["Peng Xiao <xiaoquwl@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
pypixyz = "pypixyz.main:app"


[tool.poetry.dependencies]
python = "^3.10"
typer = { extras = ["all"], version = "^0.9.0" }


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
