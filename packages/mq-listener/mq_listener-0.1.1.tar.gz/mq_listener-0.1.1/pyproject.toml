[tool.poetry]
name = "mq-listener"
version = "0.1.1"
description = ""
authors = ["sampath.raghuraman <sampath.raghuraman@intellectdesign.com>"]
readme = "README.md"
packages = [{include = "mq_listener"}]

[tool.poetry.dependencies]
python = "^3.8"
stompest = "^2.3.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
